[project]
name = "paracore-mcp-build"
version = "0.1.0"
description = "Isolated build environment for the Paracore MCP server executable"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.14.0",
    "grpcio>=1.76.0",
    "protobuf>=6.33.0",
    "pyinstaller>=6.0.0",
]
